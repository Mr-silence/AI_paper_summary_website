# Gemini CLI - 专属工作总结与上下文记忆

## 1. 角色定位与职责
*   **我的角色**: 开发 AI (Gemini CLI)。
*   **协同者 (Codex)**: 负责对我 (Gemini CLI) 输出的代码和架构进行 Review。**强制约束：我 (Gemini CLI) 绝对不能修改 `CODEX_MEMORY.md` 文件。**

## 2. 核心戒律 (Core Mandates)
*   **无损修改原则**: 在对 `Detailed_PRD.md` 或任何核心文档进行修改时，**严禁大幅删除、概括或压缩原本已存在的规格内容**。修改必须是**增量式**或**精准局部修订**。
*   **规格持久化**: 所有的正则表达式、物理数据库定义、Taxonomy 关键词集及 AI 模板必须在文档中保持物理显式，不得以“同前”、“已定义”等字样替代。

## 3. 工作进展记录

### [2026-03-24] v2.18 全量无损收口 (已完成)
*   **PRD 极致闭环 (收口版)**:
    *   **补位排除规则实装**: 明确了被拒绝论文在当期任务中永久列入“补位黑名单”，彻底封堵了重复补位的设计漏洞。
    *   **AI Block 切分契约化**: 在 Editor 和 Writer 阶段正式定义了基于 `re.split` 的多篇 Block 切分规则，补全了端到端解析的最后一块拼图。
    *   **API 细节补完**: 为 `/subscribe/verify` 接口补齐了必需的 `token` 参数定义，确保了交付契约的 100% 完整性。
    *   **严格遵循无损修改原则**: 本次更新仅针对反馈点精准补强，完全保留了 v2.17 的物理规格、正则及模板细节。

## 4. 下一步计划 (待执行)
*   **物理同步与 Parser 实现**:
    1. 按照 v2.18 物理规格同步 `schema.sql` 和 `models/domain.py`。
    2. 按照 v2.18 的 Block 切分规则与机械正则，实现 `services/ai_processor.py`。
    3. 在 `Pipeline` 中实装“补位黑名单”与“原子更新”逻辑。
*   **全链路验证**: 执行全流程集成测试。

## 4. 关键备忘录
*   **PRD 是唯一真理来源**: 严禁在 PRD 中使用占位符、省略语或模糊定义。文档必须包含全量物理规格。
*   **NULL 语义**: 在 `paper_summary` 中，Candidate 的解读字段必须显式存储为 NULL。
